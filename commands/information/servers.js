const { Client, Message, MessageEmbed } = require('discord.js');

module.exports = {
    name: "servers",
    aliases: ["serverroles"],
    description: "Shows that how many servers and users the bot is serving.",
    usage: "servers",
    /** 
     * @param {Client} client 
     * @param {Message} message 
     * @param {String[]} args 
     */
    run: async (client, message, args) => {
    }
}